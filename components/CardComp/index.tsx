import Head from "next/head";
import styles from "../../styles/Home.module.css";

export default function CardComp({ props }) {
  const stringifyLinkedinUserData = JSON.stringify(
    props.linkedinUserData,
    null,
    2
  );
  const { localizedLastName, id, localizedFirstName, ...rest } =
    props.linkedinUserData;
  const { profilePicture, ...rest2 } = rest;
  const { displayImage } = profilePicture;

  return (
    <div className={styles.container}>
      <Head>
        <title>Card page</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <h1 className={styles.title}>Card Page</h1>
      <p>{stringifyLinkedinUserData}</p>
      <p>{localizedLastName}</p>
      <p>{id}</p>
      <p>{localizedFirstName}</p>
      <p>{displayImage}</p>
    </div>
  );
}
